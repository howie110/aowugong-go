# 本项目操作约束

- 默认禁止发送任何外部通知；部署只执行构建、上传、重启和健康检查。
- 钉钉、邮件、微信等通知必须由用户在当前任务明确授权，不能因为环境变量存在就发送。
- 只允许读取本项目明确声明的配置，不使用 Codex、其他项目或父进程的全局通知变量。
- 涉及生产数据、服务停止、删除文件或修改外部系统时，先说明影响，再执行最小范围操作。
- 部署完成后只向用户报告结果，不自动向第三方群组发送消息。
